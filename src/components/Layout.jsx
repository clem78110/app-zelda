           >
              <Icon size={18} strokeWidth={1.8} />
              <span className="text-[10px] mt-0.5 font-semibold">{label}</span>
            </NavLink>
          ))}
      </nav>
    </div>
  );
};